## Project Constraints
- Pure HTML + CSS only
- No frameworks
- Reusable components
- CSS variables for all colors and spacing
- Consistent naming convention
- Accessibility considered where applicable

**Prompt**
Generate a semantic HTML5 structure for a single-page UI component library showcase.
Use header, main, section, and footer.
Do not include CSS.
Avoid unnecessary divs.

**What the AI produced**
A full HTML document with header, navigation, multiple sections for components, and a footer.

**What I accepted from the AI**
- Use of semantic elements (header, main, section, footer)
- High-level section separation for components
- Basic document structure (doctype, head, body)

**Prompt**
Create a set of CSS variables for a small UI component library.
Include colors, spacing, border radius, and typography.
Do not style any components.

**What the AI produced**
A detailed CSS docucment of the website UI component library

**What i accepted from the AI**
I accepted all output as it was in line with my needs.

**Prompts for each CSS styling**
**BUTTONS:**
Create CSS for reusable button components.
Include primary, secondary, and disabled states.
Use CSS variables.
Follow a modifier-based naming convention.
**Inputs:**
Create reusable CSS styles for form inputs.
Include default, focus, and error states.
Use CSS variables for colors, spacing, and borders.
Use a consistent modifier-based naming convention.
Do not include JavaScript.
Ensure the input can be reused across forms.
**Cards:**
Create reusable CSS styles for card components.
Cards should support a title, body text, and optional footer.
Use CSS variables for spacing, colors, and border radius.
Use a predictable class naming convention.
Do not over-style or add layout-specific assumptions.
**Barges**
Create reusable CSS styles for badge components.
Include default, success, warning, and error variants.
Use CSS variables for colors and spacing.
Badges should be inline and adaptable to different contexts.
**Alerts:**
Create reusable CSS styles for alert components.
Include info, success, warning, and error variants.
Use CSS variables for colors, spacing, and borders.
Alerts should be visually distinct but not intrusive.
Do not include dismissal logic or JavaScript.
**Navbar**
Create a reusable CSS-only navigation bar.
Include styles for logo area and navigation links.
Use flexbox for layout.
Use CSS variables for spacing and colors.
Do not include JavaScript or responsive hamburger logic.
**Modal**
Create CSS styles for a modal component.
Include overlay and modal container styles.
Use CSS variables for colors, spacing, and border radius.
Modal should be centered visually.
Do not include JavaScript or interaction logic.
**Tabs**
**Footer**

**What i accepted from the AI**
I accepted all output as it was in line with my needs.

**What i did**
Using Antigrvity Browser Control i Iterated on website designs and implementations and refined the website better i edited the color variable and some css components styling.
